# Regulartory Motif Finding 

```
usage: bruteforce.py [-h] [--input INPUT] [--k K] [--start START] [--end END]

Regulartory Motifs

optional arguments:
  -h, --help     show this help message and exit
  --input INPUT  file of sequences
  --k K          k-mer length
  --start START  starting sequence
  --end END      ending sequence
```

## optimized

```
usage: branchandbound.py [-h] [--input INPUT] [--k K] [--start START]
                         [--end END]

Regulartory Motifs

optional arguments:
  -h, --help     show this help message and exit
  --input INPUT  file of sequences
  --k K          k-mer length
  --start START  starting sequence
  --end END      ending sequence
```

